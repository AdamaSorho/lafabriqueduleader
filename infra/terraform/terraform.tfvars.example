# Project/region
project = "lafabrique-leader"
region  = "us-east-1"

# REQUIRED: unique S3 bucket name for the static site (must be globally unique)
# Example: "lafabriqueduleader-site-12345"
site_bucket_name = "<your-unique-s3-bucket-name>"

# Public site URL used inside emails and for CORS
site_url    = "http://<your-bucket>.s3-website-<region>.amazonaws.com"
cors_origin = "http://<your-bucket>.s3-website-<region>.amazonaws.com"

# FROM address used in emails (display name + address)
from_email = "Zonzerigué Leadership International <no-reply@lafabriqueduleader.com>"
# Google SMTP (use App Password on Gmail/Workspace)
smtp_host = "smtp.gmail.com"
smtp_port = 465
smtp_user = "no-reply@lafabriqueduleader.com"
smtp_pass = "<app-password>"

# Optional: where pre-order notifications are sent. If empty, FROM_EMAIL is used.
preorder_to_email = "orders@lafabriqueduleader.com"
# Optional: dedicated inboxes for keynote/coaching leads. If empty, falls back to leads_to_email or from_email.
keynote_to_email = "keynotes@lafabriqueduleader.com"
coaching_to_email = "coaching@lafabriqueduleader.com"
# Shared fallback for lead forms (keynote + coaching). Leave blank to use from_email.
leads_to_email = "leads@lafabriqueduleader.com"

# Enable the API (API Gateway + Lambda). Required for the excerpt email flow.
enable_api = true

# Secret used to sign verification links (use a long random string)
link_signing_secret = "<generate-a-long-random-string>"

# Optional: secure POST API with HMAC from a Cloudflare Worker proxy
api_shared_secret = "<generate-another-long-random-string>"

# Optional: Cloudflare Turnstile secret key for bot protection
turnstile_secret_key = "<turnstile-secret-key>"

# Optional: DynamoDB table name to store signups
# If set, Terraform will create the table with partition key `email` (S)
# and grant Lambda permission to PutItem. Leave empty to skip creating/using DynamoDB.
ddb_table = "newsletter_signups"
# Optional: dedicated table for preorders
preorders_ddb_table = "book_preorders"

# Excerpt files (upload these PDFs to your site bucket, under these keys)
excerpt_fr_s3_key = "private/excerpts/excerpt-fr.pdf"
excerpt_en_s3_key = "private/excerpts/excerpt-en.pdf"

# Note: With S3 website hosting, HTTPS on a custom domain requires Cloudflare,
# which is not used in this configuration.
